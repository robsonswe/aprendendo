---
import DisciplinaLayout from "@layouts/DisciplinaLayout.astro";
const allPosts = await Astro.glob("./*.{md,mdx}");
const featuredPosts = allPosts.filter(
  (post) => post.frontmatter.featured
);
const title = "Perícia";

---

<DisciplinaLayout posts={featuredPosts} title={title} />